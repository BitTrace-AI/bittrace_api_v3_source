[2026-03-27] Created and froze `bittrace_api_v3_source` as the clean source/commercial lane: canonical supported path is `temporal_threshold_36` frontend + `Lean-Lean` backend with two fixed persistence profiles (`quiet_scout=i1/d1/y6/r12/no-latch`, `aggressive=i1/d1/y3/r7/no-latch`); verification/parity, freeze/export, and deployment-boundary docs were brought forward; research-only items such as Lean-Deep, max/ceiling search, deep-layer search, 64-bit augmentation experiments, and architecture-comparison tooling remain outside this lane; next hardening step is to run canonical deployment-candidate prep, then parity/golden-vector verification, then editable-install and release-smoke checks.
[2026-03-27] Adjusted the supported source-lane `quiet_scout` persistence floor in `configs/persistence_quiet_scout.yaml` from `0.40` to `0.28` after the frozen `fc_i1_d1_y6_r12_nolatch` quiet reference measured `0.288462` train/val unhealthy detection in deployment-candidate replay; ranking and policy stayed unchanged so quiet healthy RED control remains primary while the intended quiet profile now qualifies cleanly.
[2026-03-27] Completed the release-hardening pass for `bittrace_api_v3_source`: added `scripts/run_release_smoke.py` with `release_smoke_summary.json` / `release_smoke_summary.md` artifacts, tightened `README.md`, `SUPPORTED_SCOPE.md`, `DEPLOYMENT_BOUNDARY.md`, `docs/VERIFICATION_WORKFLOW.md`, and new `docs/RELEASE_WORKFLOW.md`, narrowed the package story to Python 3.12 plus the `bittrace-source` executable, and updated CLI help to present only the supported source/commercial lane; bounded validation passed for `py_compile`, editable install, imports, and `bittrace-source --help`; next remaining blocker is one uninterrupted human-run full release smoke dry run because the canonical campaign leg is materially long.
[2026-03-28] Final launch cleanup: replaced the remaining user-facing `py_compile` sanity-check wording with a portable `find src scripts -type f -name '*.py' | sort` form in `docs/VERIFICATION_WORKFLOW.md`, kept release behavior unchanged, and re-ran lightweight validation with `py_compile` plus `bittrace-source --help`.
